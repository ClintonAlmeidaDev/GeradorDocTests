info:
  name: POST TITULO
  type: http
  seq: 3

http:
  method: post
  url: https://jsonplaceholder.typicode.com/posts
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "title": "Teste Bruno",
        "body": "Criando um recurso para testar a collection",
        "userId": 1,
        "clientSecret": "SEGREDO_SUPER_SECRETO"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
